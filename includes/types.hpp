#ifndef TYPES_HPP
#define TYPES_HPP

namespace server {

enum SERVER_STATUS {
	RECEIVING_REQUEST,
	PREPARING_RESPONSE,
	EXECUTING_CGI,
	RESPONSE_SENT,
	PROCESSING_ERROR,
	STATUS_UNDEFINED,
	COMPLETE,
	WAITING_FOR_NEXT_REQUEST,
};

} // namespace server

#endif
