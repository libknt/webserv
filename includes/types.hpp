#ifndef TYPES_HPP
#define TYPES_HPP

#ifndef BUFFER_SIZE
#define BUFFER_SIZE 1024
#endif

namespace server {

enum SERVER_STATUS {
	RECEIVING_REQUEST,
	PREPARING_RESPONSE,
	EXECUTING_CGI,
	RESPONSE_SENT,
	PROCESSING_ERROR,
	STATUS_UNDEFINED,
	COMPLETE,
	WAITING_FOR_NEXT_REQUEST,
};

} // namespace server

#endif
