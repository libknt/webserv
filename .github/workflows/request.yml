name: request_test

on:
  push:
    branches:
      - feature/marai-test-request
    workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a one-line-script
        run: echo hello world
